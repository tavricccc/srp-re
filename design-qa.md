# Design QA

- References: `C:/Users/tavri/Downloads/IMG_3460.PNG`, `IMG_3461.PNG`, `IMG_3462.PNG`, `IMG_3463.PNG`, `IMG_3470.PNG`, `30CB3A37-E205-457E-95EE-FE6609969504.png`, and `C:/Users/tavri/AppData/Local/Temp/codex-clipboard-86d23466-a1ce-43c6-8b9a-d7a31e029bca.png`.
- Target: correct mobile sheet padding, natural-height action menu, keyboard-stable route-based creation flows, clearly layered system-settings navigation and category editing, independently presented desktop settings/notification popups with compact balanced padding, aligned Bottom Tab/detail footer spacing, continuous route/header/detail-tab motion, slightly heavier dialog top padding, centered sidebar avatar, and retained neumorphic shadows without iOS WebKit ghost layers.
- Structural review: route pages overlap in one fixed Grid cell; hierarchy changes use a short logical-inset slide plus opacity while same-level routes crossfade. The outgoing page never switches to absolute positioning, no `out-in` blank frame is introduced, and route motion avoids transform-composited shadow layers; detail tabs remain opacity-only. Bottom Tab selection uses a static background on each item instead of a measured, translated gray indicator. Content attachments, lightbox images, editor previews and avatars share block-level `DecodedImage`, which removes inline baseline gaps and keeps the native image transparent behind a spinner until both network loading and `decode()` finish, preventing progressive top-to-bottom paint. Double-tap zoom is intercepted by coordinate-based capture touch handling plus a dblclick fallback while pinch zoom remains available. The persistent mobile header keeps one back-button node and one title-content instance; its animated slot now matches the real 44px tap target so the control cannot overflow over the title. Bottom-Tab pages remove App main-content's duplicate 1rem bottom padding, and Detail/Skeleton shells remove their extra 5px/8px padding so the route-level safe gap is the sole source of spacing above the Tab. Sheet handle removed from layout flow; padded dialogs add a small top optical adjustment; action menu uses content height; proposal/facility/announcement creation share dedicated `/new` route pages tied to Visual Viewport height and unsaved-navigation guards; AppShell padding owns their side inset and their compact bottom inset still accounts for the Home Indicator; system settings and Setup use the same equal-width feature segments and selected-category editor, keep feature switches in the batch-save draft, disable category fields while the draft feature is off, keep only the unique-default switch, and treat deletion as permanent removal of all related records; Setup explicitly confirms that manager assignment is deferred until people register; mobile category selection scrolls horizontally; desktop utility panels use compact balanced outer padding without sheet-handle compensation, while inner scroll padding reserves shadow drawing room without negative bleed; skeleton opacity runs on a shadow-free inner node; keyed list teardown no longer recreates the shadow tree.
- Automated review: `verify:local` passed; the separately rerun `verify:integration` passed 18 tests with 0 failures.
- Visual comparison: blocked because this repository's `AGENTS.md` explicitly prohibits in-app browser preview. No browser screenshot was captured.

final result: blocked
